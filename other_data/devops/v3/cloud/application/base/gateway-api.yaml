apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-api
  namespace: edu-cloud
  labels:
    app: gateway-api
    type: cloud
    k8s.kuboard.cn/layer: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway-api
      type: cloud
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: gateway-api
        type: cloud
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: type
                    operator: In
                    values:
                      - cloud
      imagePullSecrets:
        - name: harbor-secret
      containers:
        - name: gateway-api
          env:
            - name: nacos_server_addr
              valueFrom:
                configMapKeyRef:
                  name: cloud-conf
                  key: NACOS_SERVER_ADDR
            - name: env_profile
              valueFrom:
                configMapKeyRef:
                  name: cloud-conf
                  key: ENV
            - name: JAVA_OPTS
              value: '-Xms1024m -Xmx1024m -Duser.timezone=GMT+08'
            - name: log_dir
              value: /var/log/edu
            - name: opentracing.jaeger.enabled
              value: 'false'
          resources:
            limits:
              cpu: 500m
              memory: 2G
          image: core.harbor.cz.shenlan.com/dbp-edu/gateway-api:latest
          imagePullPolicy: Always
          ports:
            - name: tcp-essential
              containerPort: 8001
              protocol: TCP
          startupProbe:
            httpGet:
              path: /actuator/health
              port: tcp-essential
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              port: tcp-essential
              path: /actuator/health
            initialDelaySeconds: 30
          livenessProbe:
            httpGet:
              port: tcp-essential
              path: /actuator/health
            initialDelaySeconds: 30
          lifecycle:
            preStop:
              exec:
                command: ["/bin/bash", "-c", "sleep 20"]


---
apiVersion: v1            #必须
kind: Service             #必须
metadata:                 #必须
  name: gateway-api
  namespace: edu-cloud        #必须
  labels:
    app: gateway-api
    type: cloud
spec:                   #必须
  selector:            #必须
    app: gateway-api
    type: cloud
  type: NodePort        #必须
  ports:
    - name: tcp-essential
      protocol: TCP
      port: 8001
      targetPort: 8001
      # nodePort: 31001