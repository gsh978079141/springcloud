apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-exam-test
  namespace: education-test
  labels:
    app: service-exam
    release: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-exam
      release: test
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: service-exam
        release: test
    spec:
      nodeSelector:
        education: "true"
      imagePullSecrets:
        - name: harbor-secret
      containers:
        - name: service-exam
          image: core.harbor.cz.shenlan.com/dbp-edu/service-exam:latest
          env:
            - name: config_server_url
              value: http://server-config:8888
            - name: env_profile
              value: test
            - name: JAVA_OPTS
              value: '-Xms1024m -Xmx1024m -Duser.timezone=GMT+08'
            - name: log_dir
              value: /var/log/edu
            - name: opentracing.jaeger.enabled
              value: 'false'
          ports:
            - name: tcp-essential
              containerPort: 7003
              protocol: TCP
          readinessProbe:
            httpGet:
              port: tcp-essential
              path: /actuator/health
          lifecycle:
            preStop:
              exec:
                command: ["/bin/bash", "-c", "sleep 20"]
---
apiVersion: v1            #必须
kind: Service             #必须
metadata:                 #必须
  name: service-exam
  namespace: education-test        #必须
  labels:
    service: service-exam
spec:                   #必须
  selector:            #必须
    app: service-exam
    release: test      #必须
  type: NodePort        #必须
  ports:
    - name: tcp-essential
      protocol: TCP
      port: 7003
      targetPort: 7003