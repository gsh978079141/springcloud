apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge-service-teacher-iot
  namespace: education
  labels:
    app: edge-service-teacher-iot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edge-service-teacher-iot
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: edge-service-teacher-iot
    spec:
      tolerations:
        - key: "edge"
          operator: "Equal"
          value: "true"
          effect: "NoSchedule"
      hostNetwork: true
      imagePullSecrets:
        - name: harbor-secret
      containers:
        - name: edge-service-teacher-iot
          image: core.harbor.cz.shenlan.com/dbp-edu/edge-service-teacher-iot:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: env_profile
              valueFrom:
                configMapKeyRef:
                  name: edge-conf
                  key: env
            - name: edge_name
              valueFrom:
                configMapKeyRef:
                  name: edge-conf
                  key: edgeName
            - name: node_name
              valueFrom:
                configMapKeyRef:
                  name: edge-conf
                  key: nodeName
            - name: JAVA_OPTS
              value: '-Xms1024m -Xmx1024m -Duser.timezone=GMT+08'
            - name: log_dir
              value: /var/log/edu
            - name: opentracing.jaeger.enabled
              value: 'false'
            - name: bff_teacher_iot_url
              valueFrom:
                configMapKeyRef:
                  name: edge-conf
                  key: bffTeacherIotUrl
            - name: edge_service_media_url
              valueFrom:
                configMapKeyRef:
                  name: edge-conf
                  key: edgeServiceMediaUrl
            - name: edge_service_biz_url
              valueFrom:
                configMapKeyRef:
                  name: edge-conf
                  key: edgeServiceBizUrl
          ports:
            - name: tcp-essential
              containerPort: 9002
              protocol: TCP
          readinessProbe:
            exec:
              command:
                - wget
                - http://localhost:9002/actuator/health
                - -O
                - health
          volumeMounts:
            - mountPath: /var/log/edu
              name: persistent-log-storage
          lifecycle:
            preStop:
              exec:
                command: ["/bin/bash", "-c", "sleep 20"]
      volumes:
        - name: persistent-log-storage
          hostPath:
            type: DirectoryOrCreate
            path: /root/log/edu
