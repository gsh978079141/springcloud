#端口号
server:
  port: 8082
  servlet:
    context-path: /users
spring:
  application:
    name: service-user
  #  profiles:
  #    active: dev
  #cloud
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
#      config:
#        server-addr: 127.0.0.1:8848
#        #！注意！默认命名空间此处不需要填写，其他填空间ID
#        namespace: c3a25393-f967-4aa7-a914-ebf90282235c
#        #指定配置群组 --如果是Public命名空间 则可以省略群组配置
#        group: DEFAULT_GROUP
#        #文件名 -- 如果没有配置则默认为 ${spring.appliction.name}
#        prefix: ${spring.application.name}
#        #指定文件后缀
#        file-extension: yml
swagger:
  api-info:
    title: guansh-springcloud-api-docs
    description: 用户服务
    version: '1.0'
    contact:
      name: guanshenghui
      url: http://${swagger.host}/exam/v1/api-docs
      email: 978079141@qq.com