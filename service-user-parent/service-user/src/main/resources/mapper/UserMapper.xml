<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gsh.springcloud.user.dao.UserMapper">

    <!-- 通用查询映射结果 -->
  <resultMap id="BaseResultMap" type="com.gsh.springcloud.user.model.User">
        <id column="id" property="id" />
        <result column="username" property="userName" />
        <result column="password" property="password" />
        <result column="age" property="age" />
        <result column="integral" property="integral" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, username, password, age , integral
    </sql>
  <insert id="insertTest" parameterType="com.gsh.springcloud.user.model.User">
        insert into user(username, integral) VALUES(#{username,jdbcType=VARCHAR}, #{integral,jdbcType=INTEGER})
    </insert>
</mapper>
