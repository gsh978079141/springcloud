#端口号
server:
  port: 8081
  servlet:
    context-path: /order
spring:
  application:
    name: service-order
  #  profiles:
  #    active: dev
  #cloud
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        # 指定namespace无效，是否为容器启动nacos原因？
        # namespace: 48cf4b69-1eee-47c0-b703-cd135f8e6669
        #      config:
        #        server-addr: 127.0.0.1:8848
        #        #！注意！默认命名空间此处不需要填写，其他填空间ID
        #        namespace: '48cf4b69-1eee-47c0-b703-cd135f8e6669'
        #        #指定配置群组 --如果是Public命名空间 则可以省略群组配置
#        group: DEFAULT_GROUP
#        #文件名 -- 如果没有配置则默认为 ${spring.appliction.name}
#        prefix: ${spring.application.name}
#        #指定文件后缀
#        file-extension: yml